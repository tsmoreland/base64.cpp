#pragma once

#include <iostream>
#include <vector>
#include <optional>
#include <string>

namespace moreland::base64::converters
{
    using byte = unsigned char;
    using std::optional;
    using std::nullopt;
    using std::vector;
}
